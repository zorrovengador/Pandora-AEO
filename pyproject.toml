[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pandora-aeo"
version = "0.1.0"
description = "Deterministic AEO and Agent Readiness audits for Hermes Agent"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.scripts]
pandora-aeo = "pandora_aeo.cli:main"
